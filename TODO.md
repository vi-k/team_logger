# TODO

- stackTrace переносить на след. строку, если не вмещается.
- для Iterable параметр в конфиге, чтобы использовать
  efficientLengthIterableToString.
- При ограничении сообщения (Constraints), ellipsis "…" появляется на каждой
  строке, даже на невидимых
- дефолтный конфиг (сейчас настройки напрямую в теме) и force-конфиг для
  игнорирования настроек пользователя:
  defaultConfig <- userConfig <- forceConfig
- Обновить example и скриншот для flutter_team_log: убрать чувствительную
  информацию.
- Сделать возможным добавление собственных уровней и собственных функций
  логирования.

README:

- [ ] lazy messages
- [ ] output: print/log
- [ ] zones
- Переснять скриншот flutter_team_logger с синтетическими данными (старый удалён: содержал реальные данные).

> **Состояние на 2026-08-24:** сделано. 0.7.1 опубликована на pub.dev,
> тег `v0.7.1` = `f0a226a`.
> **Что это:** отчёт о выпуске 0.7.1 — патч из одного дефекта.
> **Связанные записи:** `2026-08-24[7]-loggable-data-name-report.md`
> (сам дефект), `2026-08-24[6]-release-0.7.0-report.md` (процедура и её
> ловушки).

# Релиз 0.7.1

Выпущена 2026-08-24 (2026-08-24T15:34:21Z на сервере). Разрешение владельца
пришло отдельным сообщением, состоящим только из слова «публикуй», —
условие `AGENTS.md` соблюдено.

## Что вошло

Один пункт: геттер `LoggableData.name` возвращал строку `'type'` вместо
имени класса. Разбор — `2026-08-24[7]-loggable-data-name-report.md`.
Публичных контрактов правка не ломает: тип возвращаемого значения тот же
`String`, менялось только значение — с заведомо неверного на то, которое
показывает вывод.

## Чем проверено перед связкой

Всё перезапущено непосредственно перед связкой:

- `dart analyze` чисто; `dart test` — 626 тестов; `dart format` — 106
  файлов без изменений;
- `example/`: `pub get`, `analyze` чисто, `dart run example.dart` отработал;
- `scripts/screenshots.sh --check` и `scripts/gallery.sh --check` — код 0;
- `dart pub publish --dry-run` **из корня** — 0 предупреждений, архив
  266 КБ.

Матрицу Flutter не пересобирал: зависимости и их ограничения не менялись,
последняя сборка — того же дня (`6e949d0`).

## Связка

`f0a226a` (`Release 0.7.1`) → тег `v0.7.1` → push обоих → `dart pub
publish`. Сервер ответил `Successfully uploaded ... version 0.7.1`;
`https://pub.dev/api/packages/team_logger` подтверждает `latest: 0.7.1`.

## Обе ловушки 0.7.0 обошли заранее

Ловушки, записанные в отчёте о 0.7.0, в этот раз были известны и учтены:

- **Пустой релизный коммит** — принят как норма осознанно. Версия и
  `CHANGELOG` поднялись вместе с правкой (`ca056ba`), поэтому `Release
  0.7.1` сделан с `--allow-empty` и в теле сказано, почему он без диффа.
  Это выбор, названный в отчёте о 0.7.0 вторым вариантом: тег указывает на
  коммит, который прямо говорит «это релиз».
- **Каталог публикации** — `dart pub publish` запущен из корня, `pwd`
  напечатан той же командой. Проверка примера шла в подоболочке
  (`(cd example && …)`), поэтому рабочий каталог сессии из корня не уходил.

## Что всплыло в самой процедуре

Ничего нового. Единственная правка по ходу — сообщение коммита с
исправлением дефекта: в первой редакции стояла строка «Released as 0.7.1»,
хотя релиза на тот момент не было. Заменена до push на явное «версия
поднята, ничего не опубликовано». Формулировки о выпуске в коммитах стоит
проверять тем же правилом, что и теги: коммит и тег, говорящие о релизе
раньше публикации, лгут.

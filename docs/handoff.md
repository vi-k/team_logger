# Текущее состояние

> Обновлено: 2026-08-20

Только «где мы сейчас». История — в `docs/records/`, пожелания владельца — в
`docs/backlog.md`, устройство пакета — в `docs/architecture.md`.

## Где мы

Ветка `main`, версия в `pubspec.yaml` — **0.7.0, ещё не выпущена**.

В 0.7.0 вошли четыре волны работ:

1. Санитайзер корневой позиции и сужение его области видимости (последний
   коммит работы — `9b0478b`, запись в CHANGELOG — `03ea23c`).
2. Обновление зависимостей: `ansi_escape_codes` 3.1.2 → 4.0.1,
   `logger_builder` 0.5.0 → 0.6.1. Стили ansi переехали из имён верхнего
   уровня в класс `Styles`, он теперь реэкспортируется из `team_logger`.
   Подробности — `docs/records/2026-08-18[1]-dependency-upgrade-report.md`.
3. Возвращена устанавливаемость во Flutter-приложение начиная с Flutter
   3.27: floor'ы `clock`/`meta`/`stack_trace` опущены до пинов 3.27, а
   `format` обновлён до ^4.1.0 — вышедшая в тот же день версия опустила
   свой floor до Dart ^3.6.0 и не тянет `intl`, так что тот ушёл из графа
   зависимостей приложений. Подробности —
   `docs/records/2026-08-18[2]-flutter-compat-report.md`.
4. **`logger_builder` 0.6.1 → 0.7.0** (2026-08-20, не закоммичено). Апстрим
   починил наследование publisher'а по уровням и ограничил очереди
   асинхронных publisher'ов; `FileLogStorage` получил `onDropped` и
   `maxQueueSize`. Подробности —
   `docs/records/2026-08-20[1]-logger-builder-0.7.0-report.md`.

Поскольку 0.7.0 не выпускалась, ступень `logger_builder` 0.6.1 в CHANGELOG
не отдельная запись: строка требования переписана на «^0.7.0, up from
^0.5.0», содержимое 0.6.x — отдельным пунктом внутри той же секции.

Осталось до релиза: связка публикации целиком — см. «Следующий шаг».

Отдельно от 0.7.0, кода не касается: заведена документация для агентов
(`AGENTS.md` как единственная точка входа, `docs/` по ролям, `docs/records/`
с историей, хук `SessionStart` в `.claude/settings.json`) и записано
правило публикации на pub.dev — `AGENTS.md`, раздел «Публикация на
pub.dev», чеклист подготовки — `docs/conventions.md`, раздел «Релиз».

## Состояние дерева

**Не закоммичено** — вся волна 4 (обновление `logger_builder` до 0.7.0):

- `pubspec.yaml` — `logger_builder: ^0.7.0`;
- `lib/src/file_storage/file_log_storage.dart` — `onDropped` и
  `maxQueueSize` в конструкторе, dartdoc про границу очереди;
- `test/logger/logger_test.dart`, `test/file_storage/file_log_storage_test.dart`
  — по одному новому тесту (419 → 421);
- `CHANGELOG.md`, `README.md`, `docs/architecture.md` — описание нового
  поведения;
- `docs/backlog.md` — удалён пункт «Фикс наследования publisher в
  `logger_builder`»: апстрим его закрыл в 0.7.0;
- `docs/records/2026-08-20[1]-logger-builder-0.7.0-report.md` — новый,
  отчёт по волне;
- `docs/handoff.md` — этот файл.

Чужих правок в дереве нет — всё перечисленное сделано в этой сессии.

**23 коммита не запушены** (`origin/main..HEAD`, от `96c7989` до
`0cd5752`) — push не делался, потому что по рабочему циклу его предлагают
владельцу, а не выполняют молча.

## Статус проверок (2026-08-20)

- `dart test` — 421 тест, все зелёные.
- `dart analyze` — чисто и в корне, и в `example/`, без единого замечания.
- `dart format` — чисто.
- `dart pub publish --dry-run` — единственное предупреждение про
  незакоммиченные файлы (ожидаемо для грязного дерева); на чистом дереве
  было 0 предупреждений, архив 145 КБ.
- На настоящем Dart 3.6 (SDK внутри fvm-сборки Flutter 3.27) — `analyze`
  чист, 421 тест зелёный. Прогонять приходится копией пакета без
  `example/`: тот тянет `build_runner` >=2.5.0, которому нужен Dart 3.7,
  так что из корня `pub get` на 3.6 не резолвится.
- `dart pub outdated` — пусто: `dart pub upgrade` после бампа подтянул
  `archive` 4.1.0 и `vm_service` 15.3.0. Ограничения в `pubspec.yaml` не
  менялись, `pubspec.lock` в `.gitignore`.
- `dart pub downgrade` (минимальные версии всего) НЕ собирается — ни на
  3.6, ни на 3.13. Причина не в наших floor'ах: минимальный `glob` 2.0.0
  тянет `file` 6.0.0, который не компилируется с современным `dart:io`
  (в `File.create` появился `exclusive`). Лечится строкой `glob: ^2.1.0`
  в `dev_dependencies`, если понадобится CI-джоба «minimum versions».
- `example/`: `bin/example.dart` отрабатывает, 374 строки; вывод сверен с
  до-обновленческим (дерево `git archive HEAD` + logger_builder 0.6.1) и
  отличается только миллисекундами во времени логов.

## Матрица Flutter (переснята вручную 2026-08-20)

`flutter pub get` в пробном приложении с path-зависимостью, с
`flutter_localizations` и без: Flutter 3.27.0, 3.29.0, 3.44.4, 3.47.0
(stable) — все восемь комбинаций проходят. На 3.27 + `flutter_localizations`
в lock'е `logger_builder` 0.7.0 и `intl` 0.19.0 (последний — от
flutter_localizations, не от нас). Пробные приложения лежали в scratchpad'е
сессии; при изменении зависимостей матрицу надо переснять.

## Зависимости, оставшиеся позади

Упёрлись в floor Dart `^3.6.0` (он же Flutter 3.27) — и только среди
dev-зависимостей, потребителей это не касается. `lints` и `test` заданы
диапазонами, так что версия подбирается по SDK разработчика: на 3.13 это
`lints` 6.1.0 и `test` 1.31.2, на 3.6 — 5.1.1 и 1.26.3. Обе конфигурации
проверены: analyze чист, 421 тест зелёный. Всё остальное — на последних
версиях.

## Известные ограничения и техдолг

Найдено ревью, сознательно отложено:

- Ширина wide-символов (CJK, эмодзи) считается в code units — колонки в
  консольном выводе съезжают; нужен учёт восточноазиатской ширины.
- Все свойства builder'а выброшены правилом — блок данных остаётся с
  висящим двоеточием (`login: `). Для multi-data это починено (обход
  секций сообщает о дропе), для `LoggableData` аналога нет: пустой рендер и
  «всё выброшено» неразличимы (находка кросс-ревью 0.7.0).
- Наследование config корневой заменой сделано только для
  `LoggableMultiData` и `LoggableWrapper`; `Loggable.builder(config:)` и
  `mapBuilder(config:)` свой config замене не передают. Не регрессия (так
  было всегда) и все рендереры согласованы между собой, но после введения
  политики «config описывает способ печати и наследуется» выглядит
  непреднамеренным пробелом.
- `FileLogStorage` с упавшей инициализацией (`_disabled`) выбрасывает логи
  молча: в `onDropped` они не идут, туда попадает только отказ по
  `maxQueueSize`. Само состояние сообщается через `onError` один раз.

Ограничение про точечный override уровня («отвязывает подлоггер целиком»)
снято: `logger_builder` 0.7.0 его починил, у нас на это есть регрессионный
тест в `test/logger/logger_test.dart`.

## Найдено попутно и НЕ починено

`example/bin/readme_examples/data.dart` падает — `InvalidSpecifierException`
из `format` на спецификаторе `,n` (`README.md`, строки ~999–1003, и тот же
блок в примере: «Integer number (Bengali locale)» с
`Intl.defaultLocale = 'bn'`). Это хвост волны 3, а не волны 4: проверено на
дереве до обновления `logger_builder` — падает точно так же. `format`
4.1.0 больше не тянет `intl`, и локалезависимый `n` для целых у него
невалиден, а README и пример всё ещё ему учат. Библиотека тут ни при чём:
`Loggable._intToString` просто передаёт спецификатор пользователя внутрь
`format`.

Утверждение прошлого handoff'а «все `bin/readme_examples/*.dart`
отрабатывают» было неверным. Остальные семь отрабатывают.

Чинить — отдельная работа: надо решить, чем заменить пример с локалью
(intl из графа зависимостей убран намеренно), и поправить README и
`example/`. В 0.7.0 это уехало бы как рабочий пример, который бросает
исключение, так что до релиза стоит закрыть.

## Следующий шаг

Волна 4 закончена и проверена, но **не закоммичена** — предложить владельцу
коммит и push (23 старых коммита + новый).

0.7.0 после этого снова готова к публикации: подготовка (пункты 1–6
чеклиста «Релиз» в `docs/conventions.md`) сделана, dry-run чист на чистом
дереве. Остаётся связка «коммит `Release 0.7.0` — тег `v0.7.0` — push —
`dart pub publish`». Запускать её нельзя, пока не придёт отдельный запрос
владельца, состоящий только из просьбы опубликовать (`AGENTS.md`,
«Публикация на pub.dev»). Разрешения не спрашивать, вариантов не
предлагать.

Пока его нет — работать по `docs/backlog.md`.

В соседнем репозитории `~/development/my/logger_builder` идёт своя работа.
Это не наше — не трогать; мы его только потребляем с pub.dev.

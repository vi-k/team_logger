# Текущее состояние

> Обновлено: 2026-08-17

Только «где мы сейчас». История — в `docs/records/`, пожелания владельца — в
`docs/backlog.md`, устройство пакета — в `docs/architecture.md`.

## Где мы

Ветка `main`, версия в `pubspec.yaml` — **0.7.0, ещё не выпущена**.

Работа над 0.7.0 закончена в коде: волна правок по кросс-ревью 0.6.0
(санитайзер корневой позиции на голом `toString()`, вывод
message/error/stackTrace/tags/path из области видимости правила, замена
больше не наследует `units`, однократный рендер ключа `Map`, явный сигнал
дропа для публишеров). Последний коммит работы — `9b0478b`.

Осталось до релиза: закоммитить релизную подготовку, `git push`,
`dart pub publish` (перед публикацией показать владельцу дифф).

## Незакоммиченное в рабочем дереве

Правки прошлой сессии, **не мои** — коммитить отдельно от изменений
документации:

- `CHANGELOG.md` — запись 0.7.0 (48 строк);
- `pubspec.yaml` — версия `0.6.0` → `0.7.0`.

## Статус проверок (2026-08-17)

- `dart test` — 396 тестов, все зелёные.
- `dart analyze` — чисто по коду; одно предупреждение по конфигу:
  `analysis_options.yaml:6:5 Invalid format for the 'exclude' section`
  (секция `exclude` записана как map `"web/**": true`, анализатор ждёт
  список). Мелочь, руки не дошли.

## Известные ограничения и техдолг

Найдено ревью, сознательно отложено:

- Ширина wide-символов (CJK, эмодзи) считается в code units — колонки в
  консольном выводе съезжают; нужен учёт восточноазиатской ширины.
- Все свойства builder'а выброшены правилом — блок данных остаётся с
  висящим двоеточием (`login: `). Для multi-data это починено (обход
  секций сообщает о дропе), для `LoggableData` аналога нет: пустой рендер и
  «всё выброшено» неразличимы (находка кросс-ревью 0.7.0).
- Наследование config корневой заменой сделано только для
  `LoggableMultiData` и `LoggableWrapper`; `Loggable.builder(config:)` и
  `mapBuilder(config:)` свой config замене не передают. Не регрессия (так
  было всегда) и все рендереры согласованы между собой, но после введения
  политики «config описывает способ печати и наследуется» выглядит
  непреднамеренным пробелом.

## Следующий шаг

Спросить владельца, релизить ли 0.7.0. Если да — коммит релизной подготовки
(`CHANGELOG.md` + `pubspec.yaml`), push, публикация. Если нет — вернуться к
`docs/backlog.md` и предложить оттуда пункт.

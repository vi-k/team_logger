# Текущее состояние

> Обновлено: 2026-08-24

Только «где мы сейчас». История — в `docs/records/`, пожелания владельца — в
`docs/backlog.md`, устройство пакета — в `docs/architecture.md`, правила
работы — в `AGENTS.md`.

## Где мы

Ветка `main`, рабочее дерево **чистое**, незавершённой работы нет, чужих
правок в дереве нет. Всё запушено в `origin/main` (2026-08-24).

Последняя работа — **дефект `LoggableData.name`**, коммит `ca056ba`:
геттер возвращал строку `'type'` вместо имени класса (читал `TypeProp.name`
— ключ свойства, всегда `'type'`, — пока сеттер писал в
`TypeProp.typeName`). Теперь геттер возвращает `typeName ?? тип`, и на него
же переведены оба внутренних вычисления имени; тесты —
`test/loggable/loggable_data_test.dart`. Разбор —
`2026-08-24[7]-loggable-data-name-report.md`.

**0.7.0 выпущена** 2026-08-24: тег `v0.7.0` = `fbf10cd`, на pub.dev
`latest: 0.7.0`. Отчёт — `2026-08-24[6]-release-0.7.0-report.md`.
**0.7.1 подготовлена, но не выпущена:** версия поднята в `pubspec.yaml` и
описана в `CHANGELOG.md`, тега `v0.7.1` нет, на pub.dev её нет.

Следующая публикация — снова **только по отдельному запросу владельца,
состоящему только из просьбы опубликовать** (`AGENTS.md`, раздел
«Публикация на pub.dev»). Разрешение на 0.7.0 на неё не переносится:
не спрашивать, не предлагать.

## Что осталось

1. **`logger_builder` → 1.0** — соседний репозиторий, подготовка там
   закончена. См. «Соседний репозиторий» ниже. Нас не блокирует: мы
   потребляем 0.8.0 с pub.dev.
2. **1.0 этого пакета** — со стороны кода план `2026-08-24[2]` пройден
   целиком, поверхность заморожена и задокументирована. Открытых работ нет;
   следующий шаг — решение владельца, когда резать 1.0.
3. **0.7.1** — закоммичена и запушена, ждёт публикации (только по
   отдельному запросу владельца).

Проверок незакрытых нет, `docs/backlog.md` пуст, все находки ревью
разобраны.

## Статус проверок (2026-08-24, всё зелёное)

- `dart test` — 626 тестов (+6 на `LoggableData.name`).
- `dart analyze` — чисто в корне и в `example/`.
- `dart format --output=none --set-exit-if-changed .` — чисто, 106 файлов.
- `dart pub publish --dry-run` — архив **266 КБ**, предупреждений нет
  (перезамерено 2026-08-24 после правки `LoggableData.name`; запускать
  **из корня**: из `example/` он проверяет пример, а не пакет).
- `scripts/screenshots.sh --check` — код 0, все 45 кадров байт-в-байт.
- `scripts/gallery.sh --check` — чисто (4 WebP для карточки pub.dev).
- `dart doc --validate-links` — 0 предупреждений, 0 ошибок.
- Dart 3.6 (SDK внутри fvm-сборки Flutter 3.27) — **перезамерено
  2026-08-24** после подъёма `logger_builder`: `pub get` резолвится
  (`logger_builder` 0.8.0, `characters` 1.4.1), analyze чист, 620 тестов
  зелёные. Копию делать без `example/` и `tool/`, но **с** `gallery/`, и
  удалять в ней `pubspec.lock` — подробности в `docs/architecture.md`.
- `dart pub outdated` — пусто (замерено 2026-08-22).
- **Матрица Flutter — пересобрана 2026-08-24** после подъёма
  `logger_builder`: `flutter pub get` в пробном приложении с
  path-зависимостью, с `flutter_localizations` и без, на 3.27.0, 3.29.0,
  3.44.4 и 3.47.0 (stable) — 8 из 8, везде `logger_builder` 0.8.0.
  Констрейнт `characters ^1.3.0` перепроверен негативной пробой на 3.27.
  Подробности и разбор пинов — в `docs/architecture.md`.

Полный прогон из корня:

```bash
dart analyze && dart test && dart format --output=none --set-exit-if-changed .
dart pub publish --dry-run
scripts/screenshots.sh --check && scripts/gallery.sh --check
cd example && dart pub get && dart analyze && dart run example.dart
```

## Правила, которые легко нарушить

- **`README.ru.md` правится тем же коммитом, что `README.md`.** Из
  публикации его убирает одна строка в `.pubignore` — не удалять.
- **Разделы README 1–11 не перенумеровывать:** на «README §10» ссылаются три
  комментария в `test/loggable/sanitizer_cross_review_test.dart`. Новое
  добавлять довеском (сейчас есть §12 и §13).
- **В записи ревью вердикт ровно один и текущий**, прозу находки не трогать
  (`docs/conventions.md`, «Записи ревью»).
- **В `docs/backlog.md` агент не пишет** — только читает и удаляет сделанное.
- **По-русски ровно три места:** `docs/`, `README.ru.md`, `AGENTS.md`
  (+`CLAUDE.md`, который только подключает его). Всё остальное, включая
  комментарии в конфигах, — по-английски. Исключение — 12 строк
  синтетических данных в `tool/playground_data.dart`: это данные, а не
  комментарии.
- **Политики заморозки API** — `docs/conventions.md`, раздел «Заморозка API
  на 1.0». Главное: **консольный рендер не замораживается**; формат JSONL —
  замораживается и версионируется (`FileLogCodec.formatVersion`).
- **Состояние API-справочника проверять на
  `pub.dev/documentation/<пакет>/latest/`**, а не локальным `dart doc`: он
  не разрешает элементы зависимостей, и на этом уже была снята неверная
  находка.
- **Списки ограничений стареют молча.** Переписывая или перенося их —
  проверять запуском, а не переносить на веру.

## История — где искать

| Работа | Запись |
| --- | --- |
| Полное ревью пакета: 16 находок | `2026-08-21[1]-project-review.md` |
| `FileLogStorage`: symlink, durability, size targets, gzip | `2026-08-21[4]…[7]` |
| Пять Low-находок | `2026-08-22[4]-low-findings-report.md` |
| Цепочка конфигов `default ← user ← force` | `2026-08-22[6]-config-chain-report.md` |
| Безопасный режим ANSI (по умолчанию включён, ~+1,6% на рендере) | `2026-08-22[8]-ansi-safe-mode-report.md` |
| `iterableEfficientLength` | `2026-08-22[9]` |
| Обрезка `Map` лимитами (счётчик у **всех** мапп — выбор владельца) | `2026-08-23[2]` |
| Пробелы README; `oneCallPerLog` | `2026-08-23[3]`, `[4]` |
| Английский во всём коде | `2026-08-23[5]-english-only-report.md` |
| Сжатие handoff, куда что переехало | `2026-08-23[6]-handoff-reset.md` |
| **Ревью глазами пользователя** — 12 вердиктов, все разобраны | `2026-08-23[7]-user-eyes-review.md` |
| README-волна по нему: план и отчёт | `2026-08-23[8]`, `[9]` |
| Раскладка по умолчанию у `ConsoleLogPrinter` | `2026-08-24[1]-default-rows-spec.md` |
| **План до 1.0** — пройден со стороны пакета | `2026-08-24[2]-road-to-1.0.md` |
| Курация реэкспорта `logger_builder`: 27 имён → 9 | `2026-08-24[3]` |
| Галерея скриншотов на pub.dev | `2026-08-24[4]` |
| **Ревизия поверхности перед заморозкой**: 70 имён → 67 | `2026-08-24[5]-api-freeze-review.md` |
| **Релиз 0.7.0**: связка, матрица, две ловушки процедуры | `2026-08-24[6]-release-0.7.0-report.md` |
| **Дефект `LoggableData.name`**: разбор и правка (0.7.1) | `2026-08-24[7]-loggable-data-name-report.md` |

## Соседний репозиторий

`~/development/my/logger_builder` — свой репозиторий владельца, тот же
издатель, мы потребляем его с pub.dev (сейчас `^0.8.0`).

**Туда не лезть без явного разрешения.** 2026-08-24 там параллельно работал
другой агент; я правил запись, не перечитав её состояние, и испортил чужой
вердикт — пришлось откатывать. Владелец тогда сказал выйти оттуда.

Что там сделано и относится к нам: ревью заморозки API
(`logger_builder/docs/records/2026-08-24[1]-api-freeze-review.md`), `HasFlush`
удалён в 0.8.0, политики заморозки записаны в его `docs/conventions.md` §6 —
форма наших политик взята оттуда.

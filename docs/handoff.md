# Текущее состояние

> Обновлено: 2026-08-20

Только «где мы сейчас». История — в `docs/records/`, пожелания владельца — в
`docs/backlog.md`, устройство пакета — в `docs/architecture.md`.

## Где мы

Ветка `main`, версия в `pubspec.yaml` — **0.7.0, ещё не выпущена**. Рабочее
дерево чистое, `origin/main` совпадает с `HEAD`, незапушенного нет — сверь
`git status` и `git log --oneline -10`, как велит `AGENTS.md`.

Незавершённых работ нет: ничего не начато и брошено на середине.

## Что такое 0.7.0

Версия копилась долго и ни разу не выпускалась, поэтому в CHANGELOG она
одна большая секция, а не цепочка. Что в ней важно знать, не читая
`docs/records/` целиком:

- **Санитайзер** (`Loggable.sanitizer`) получил корневую позицию и
  суженную область видимости: `'$obj'` теперь проходит через правило, а
  сообщение, ошибка, стек, теги и путь — намеренно нет.
- **Три зависимости обновлены до ломающих версий**:
  `ansi_escape_codes` 4.0.1 (стили переехали в класс `Styles`),
  `logger_builder` 0.7.0 (починено наследование publisher'а по уровням,
  очереди асинхронных publisher'ов ограничены), `format` 4.1.0.
- **`format` больше не зависимость пакета.** Форматирование чисел —
  точка расширения темы: `LoggableConfig.intFormat`/`doubleFormat` хранят
  непрозрачный шаблон, исполняет его `LogMainTheme.numberFormatter`,
  который ставит пользователь. Тема без форматтера шаблон игнорирует.
- **Пакет снова ставится во Flutter-приложение** начиная с Flutter 3.27, в
  том числе локализованное: `intl` ушёл из графа зависимостей.

Записи за 2026-08-18 и 2026-08-20 в `docs/records/` разбирают каждую из
этих работ; имена файлов говорят сами за себя.

## Как проверить, что всё цело

Из корня репозитория:

```bash
dart analyze && dart test && dart format --output=none --set-exit-if-changed .
dart pub publish --dry-run
scripts/screenshots.sh --check
cd example && dart pub get && dart analyze && dart run example.dart
```

## Статус проверок (2026-08-20, всё зелёное)

- `dart test` — 434 теста.
- `dart analyze` — чисто в корне и в `example/`.
- `dart format` — чисто.
- `dart pub publish --dry-run` — **0 предупреждений**, архив 152 КБ.
- `scripts/screenshots.sh --check` — код 0: 41 кадр, ни одного `skip:`,
  сирот нет; повторная пересборка не меняет ни одного файла.
- На настоящем Dart 3.6 (SDK внутри fvm-сборки Flutter 3.27) — `analyze`
  чист, 434 теста зелёные. Прогонять приходится копией пакета без
  `example/` и `tool/`: пример тянет `build_runner`, которому нужен Dart
  3.7, так что из корня `pub get` на 3.6 не резолвится.
- `dart pub outdated` — пусто.
- Запускается всё: `example/example.dart`,
  `example/bin/file_storage_example.dart`, восемь
  `example/bin/readme_examples/*.dart`, `tool/playground.dart` из корня.

## Матрица Flutter (переснята вручную 2026-08-20)

`flutter pub get` в пробном приложении с path-зависимостью, с
`flutter_localizations` и без: Flutter 3.27.0, 3.29.0, 3.44.4, 3.47.0
(stable) — все восемь комбинаций проходят. На 3.27 + `flutter_localizations`
в lock'е `logger_builder` 0.7.0 и `intl` 0.19.0 (последний — от
flutter_localizations, не от нас); `format` из графа потребителя ушёл
совсем.

Зависимости пакета с тех пор не менялись — `ansi_escape_codes`, `archive`,
`clock`, `logger_builder`, `meta`, `stack_trace`, — поэтому матрица
действительна. Переснимать при любом изменении `dependencies`; пробные
приложения живут в scratchpad'е сессии и не сохраняются.

## Как устроена работа с примером и картинками

Это менялось 2026-08-20, и старые записи в `docs/records/` ссылаются на
пути, которых больше нет.

- **`example/example.dart`** — главный пример, self-contained, один запрос
  к API. Именно его показывает pub.dev на вкладке Example; `bin/` он не
  смотрит.
- **`example/bin/readme_examples/*.dart`** — восемь групп кадров для
  картинок README. Файл группы объявляет карту «имя картинки → функция» и
  отдаёт её `runFrames`: `--list` печатает имена, `<имя>` исполняет один
  кадр под фиксированными часами, без аргументов — все подряд.
- **`scripts/screenshots.sh`** пересобирает картинки из кадров
  (`--only <группа>`, `--check`). Кадр снимается отдельным процессом,
  рядом с PNG кладётся читаемый `.ansi`. Пересборка воспроизводима до
  байта. Вне конвейера — только `screenshots/flutter_team_logger.png`.
- **`tool/playground.dart`** — та самая «всё подряд» программа, которой
  раньше был `example/bin/example.dart`. Запускается из корня, в
  `.pubignore`, в архив не идёт.

## Зависимости, оставшиеся позади

Упёрлись в floor Dart `^3.6.0` (он же Flutter 3.27) — и только среди
dev-зависимостей, потребителей это не касается. `lints` и `test` заданы
диапазонами, так что версия подбирается по SDK разработчика: на 3.13 это
`lints` 6.1.0 и `test` 1.31.2, на 3.6 — 5.1.1 и 1.26.3. Обе конфигурации
проверены.

`dart pub downgrade` (минимальные версии всего) НЕ собирается — ни на 3.6,
ни на 3.13. Причина не в наших floor'ах: минимальный `glob` 2.0.0 тянет
`file` 6.0.0, который не компилируется с современным `dart:io` (в
`File.create` появился `exclusive`). Лечится строкой `glob: ^2.1.0` в
`dev_dependencies`, если понадобится CI-джоба «minimum versions».

## Известные ограничения и техдолг

Найдено ревью, сознательно отложено:

- Ширина wide-символов (CJK, эмодзи) считается в code units — колонки в
  консольном выводе съезжают; нужен учёт восточноазиатской ширины.
- Все свойства builder'а выброшены правилом — блок данных остаётся с
  висящим двоеточием (`login: `). Для multi-data это починено (обход
  секций сообщает о дропе), для `LoggableData` аналога нет: пустой рендер
  и «всё выброшено» неразличимы.
- Наследование config корневой заменой сделано только для
  `LoggableMultiData` и `LoggableWrapper`; `Loggable.builder(config:)` и
  `mapBuilder(config:)` свой config замене не передают. Не регрессия, но
  после введения политики «config описывает способ печати и наследуется»
  выглядит непреднамеренным пробелом.
- `FileLogStorage` с упавшей инициализацией (`_disabled`) выбрасывает логи
  молча: в `onDropped` они не идут, туда попадает только отказ по
  `maxQueueSize`. Само состояние сообщается через `onError` один раз.
- Локалезависимого форматирования чисел у пакета больше нет вовсе:
  `format` читает `NumberLocale` из инстанса `Format`, а тема зовёт
  топ-уровневый `format()`. Вернуть — это фича, не правка документации.

## Следующий шаг

**Работы в очереди нет.** Всё названное сделано; предлагать владельцу
следующий пункт — из `docs/backlog.md` (там пожелания владельца: конфиг
для `Iterable`, default/force-конфиги, свои уровни логирования, обрезка
`Map` лимитами, защита от ANSI-инъекций, плюс три README-темы).

0.7.0 готова к публикации: подготовка (пункты 1–6 чеклиста «Релиз» в
`docs/conventions.md`) сделана и подтверждена dry-run'ом. Остаётся связка
«коммит `Release 0.7.0` — тег `v0.7.0` — push — `dart pub publish`».
Запускать её нельзя, пока не придёт отдельный запрос владельца, состоящий
только из просьбы опубликовать (`AGENTS.md`, «Публикация на pub.dev»).
Разрешения не спрашивать, вариантов не предлагать, вопроса «публиковать?»
не задавать — в такой ситуации правило требует отказать и назвать условие.

В соседнем репозитории `~/development/my/logger_builder` идёт своя работа.
Это не наше — не трогать; мы его только потребляем с pub.dev.
